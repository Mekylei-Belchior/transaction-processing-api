<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.6</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.mekylei</groupId>
    <artifactId>transaction-processing</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>transaction-processing-api</name>
    <description>transaction-processing-api</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>

    <properties>
        <java.version>21</java.version>
        <flyway.version>12.6.2</flyway.version>
        <flyway-postgresql.version>12.6.2</flyway-postgresql.version>
        <postgresql.version>42.7.11</postgresql.version>
        <springdoc-openapi.version>3.0.3</springdoc-openapi.version>
        <bucket4j-core.version>8.19.0</bucket4j-core.version>
        <logstash-logback.version>9.0</logstash-logback.version>
        <jacoco.version>0.8.15</jacoco.version>
        <jacoco.check.skip>false</jacoco.check.skip>
        <archunit.version>1.4.2</archunit.version>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${flyway.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway-postgresql.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-core</artifactId>
            <version>${bucket4j-core.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-json</artifactId>
        </dependency>

        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>${logstash-logback.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-kafka</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-kafka</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>@{argLine} -javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar</argLine>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>

                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <formats>
                                <format>HTML</format>
                                <format>XML</format>
                                <format>CSV</format>
                            </formats>
                            <excludes>
                                <!-- Bootstrap da aplicacao Spring Boot, sem regra de negocio propria. -->
                                <exclude>**/TransactionProcessingApiApplication.class</exclude>
                                <!-- Configuracoes Spring, Kafka, Security, filtros e beans de infraestrutura. -->
                                <exclude>**/configuracao/**</exclude>
                                <!-- Entidades JPA sem comportamento de dominio relevante para cobertura. -->
                                <exclude>**/infraestrutura/entidade/**</exclude>
                                <!-- Interfaces Spring Data implementadas pelo framework em tempo de execucao. -->
                                <exclude>**/infraestrutura/repositorio/**</exclude>
                                <!-- Constantes compartilhadas sem comportamento executavel. -->
                                <exclude>**/compartilhado/constantes/**</exclude>
                                <!-- Handler HTTP generico, acoplado ao ciclo de excecoes do Spring MVC. -->
                                <exclude>**/compartilhado/exception/GlobalExceptionHandler.class</exclude>
                                <!-- DTOs de borda HTTP sem logica de negocio. -->
                                <exclude>**/*DTO.class</exclude>
                                <exclude>**/*Requisicao.class</exclude>
                                <exclude>**/*Resposta.class</exclude>
                                <!-- Pacotes dedicados a DTOs de controle e contratos externos. -->
                                <exclude>**/dto/**</exclude>
                                <!-- Adaptadores Jackson e integracoes de serializacao acopladas ao framework. -->
                                <exclude>**/compartilhado/adaptador/**</exclude>
                                <!-- Integracao Logback, de baixo valor para cobertura isolada. -->
                                <exclude>**/observabilidade/logging/**</exclude>
                                <!-- Consumidores Kafka condicionais por app.eventos.kafka.enabled=true; nos testes de integracao atuais Kafka fica desabilitado. -->
                                <exclude>**/mensageria/consumidor/**</exclude>
                                <!-- Produtor Kafka condicional por app.eventos.kafka.enabled=true; sera coberto quando Testcontainers Kafka for habilitado. -->
                                <exclude>**/mensageria/produtor/**</exclude>
                                <!-- Implementacao Kafka do publicador de outbox, tambem condicional; DominioEventoOutboxPublicador permanece incluido. -->
                                <exclude>**/mensageria/outbox/EventoOutboxPublicador.class</exclude>
                                <!-- Bounded context de cliente planejado, ainda sem comportamento implementado. -->
                                <exclude>**/cliente/**</exclude>
                                <!-- Bounded contexts PIX, TED e TEF planejados como pacotes top-level. -->
                                <exclude>**/pix/**</exclude>
                                <exclude>**/ted/**</exclude>
                                <exclude>**/tef/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>

                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <skip>${jacoco.check.skip}</skip>
                            <excludes>
                                <!-- Bootstrap da aplicacao Spring Boot, sem regra de negocio propria. -->
                                <exclude>**/TransactionProcessingApiApplication.class</exclude>
                                <!-- Configuracoes Spring, Kafka, Security, filtros e beans de infraestrutura. -->
                                <exclude>**/configuracao/**</exclude>
                                <!-- Entidades JPA sem comportamento de dominio relevante para cobertura. -->
                                <exclude>**/infraestrutura/entidade/**</exclude>
                                <!-- Interfaces Spring Data implementadas pelo framework em tempo de execucao. -->
                                <exclude>**/infraestrutura/repositorio/**</exclude>
                                <!-- Constantes compartilhadas sem comportamento executavel. -->
                                <exclude>**/compartilhado/constantes/**</exclude>
                                <!-- Handler HTTP generico, acoplado ao ciclo de excecoes do Spring MVC. -->
                                <exclude>**/compartilhado/exception/GlobalExceptionHandler.class</exclude>
                                <!-- DTOs de borda HTTP sem logica de negocio. -->
                                <exclude>**/*DTO.class</exclude>
                                <exclude>**/*Requisicao.class</exclude>
                                <exclude>**/*Resposta.class</exclude>
                                <!-- Pacotes dedicados a DTOs de controle e contratos externos. -->
                                <exclude>**/dto/**</exclude>
                                <!-- Adaptadores Jackson e integracoes de serializacao acopladas ao framework. -->
                                <exclude>**/compartilhado/adaptador/**</exclude>
                                <!-- Integracao Logback, de baixo valor para cobertura isolada. -->
                                <exclude>**/observabilidade/logging/**</exclude>
                                <!-- Consumidores Kafka condicionais por app.eventos.kafka.enabled=true; nos testes de integracao atuais Kafka fica desabilitado. -->
                                <exclude>**/mensageria/consumidor/**</exclude>
                                <!-- Produtor Kafka condicional por app.eventos.kafka.enabled=true; sera coberto quando Testcontainers Kafka for habilitado. -->
                                <exclude>**/mensageria/produtor/**</exclude>
                                <!-- Implementacao Kafka do publicador de outbox, tambem condicional; DominioEventoOutboxPublicador permanece incluido. -->
                                <exclude>**/mensageria/outbox/EventoOutboxPublicador.class</exclude>
                                <!-- Bounded context de cliente planejado, ainda sem comportamento implementado. -->
                                <exclude>**/cliente/**</exclude>
                                <!-- Bounded contexts PIX, TED e TEF planejados como pacotes top-level. -->
                                <exclude>**/pix/**</exclude>
                                <exclude>**/ted/**</exclude>
                                <exclude>**/tef/**</exclude>
                            </excludes>
                            <rules>
                                <rule>
                                    <!-- Regra global: minimo absoluto de cobertura do projeto. -->
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.60</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <!-- Regra por camada de dominio: pacotes de dominio de transacao e conta. -->
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com/mekylei/transactionprocessing/transacao/dominio*</include>
                                        <include>com/mekylei/transactionprocessing/conta/dominio*</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.75</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <!-- Regra por camada de aplicacao: servicos de transacao e conta. -->
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com/mekylei/transactionprocessing/transacao/aplicacao/servico*</include>
                                        <include>com/mekylei/transactionprocessing/conta/aplicacao/servico*</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>

                    <!-- TODO: habilitar para separar cobertura de testes de integracao em fase propria.
                         Os testes *IntegrationTest.java ja existem e hoje sao executados pelo Surefire,
                         pois o padrao **/*Test.java os inclui. Para ativar o Failsafe futuramente,
                         adicione excludes no Surefire para **/*IntegrationTest.java e includes no
                         Failsafe para **/*IntegrationTest.java. As execucoes abaixo preparam relatorios
                         separados de cobertura unitaria e de integracao usando o padrao real do projeto.

                    <execution>
                        <id>prepare-agent-integration</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                    </execution>

                    <execution>
                        <id>report-integration</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report-integration</goal>
                        </goals>
                        <configuration>
                            <formats>
                                <format>HTML</format>
                                <format>XML</format>
                                <format>CSV</format>
                            </formats>
                        </configuration>
                    </execution>
                    -->
                </executions>
            </plugin>

            <!-- TODO: habilitar para separar cobertura de testes de integracao em fase propria.
                 Os testes *IntegrationTest.java ja existem e atualmente sao executados pelo Surefire.
                 Ao ativar este plugin, configure tambem o Surefire para excluir **/*IntegrationTest.java. -->
            <!--
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>**/*IntegrationTest.java</include>
                    </includes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            -->
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>coverage</id>
            <properties>
                <!-- Desabilita apenas o check do JaCoCo para analise local sem falhar o build. -->
                <jacoco.check.skip>true</jacoco.check.skip>
            </properties>
        </profile>
    </profiles>

</project>
